const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>observer.observe(e));const menu=document.querySelector('.menu'),links=document.querySelector('.links');menu.addEventListener('click',()=>{const open=links.classList.toggle('open');menu.setAttribute('aria-expanded',open)});links.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{links.classList.remove('open');menu.setAttribute('aria-expanded','false')}));const roles=['Full-stack Developer','Backend Builder','AI Researcher'];let ri=0,ci=0,del=false;const role=document.querySelector('#role');function type(){const word=roles[ri];role.textContent=word.slice(0,ci);if(!del&&ci<word.length){ci++;setTimeout(type,70)}else if(!del){del=true;setTimeout(type,1800)}else if(ci>0){ci--;setTimeout(type,35)}else{del=false;ri=(ri+1)%roles.length;setTimeout(type,300)}}if(!reduced)type();const mouse=document.querySelector('.mouse-glow');if(!reduced){document.addEventListener('mousemove',e=>{mouse.style.left=e.clientX+'px';mouse.style.top=e.clientY+'px'});document.querySelectorAll('.card,.project').forEach(card=>{card.addEventListener('mousemove',e=>{const r=card.getBoundingClientRect();const x=(e.clientX-r.left)/r.width-.5;const y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${y*-2.5}deg) rotateY(${x*2.5}deg) translateY(-6px)`});card.addEventListener('mouseleave',()=>{card.style.transform=''})})}const progress=document.querySelector('.scroll-progress');addEventListener('scroll',()=>{progress.style.width=(scrollY/(document.documentElement.scrollHeight-innerHeight)*100)+'%'},{passive:true});
